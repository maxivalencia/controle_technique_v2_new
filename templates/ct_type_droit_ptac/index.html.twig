{% extends 'base.html.twig' %}

{% block title %}Liste Type Droit PTAC{% endblock %}

{% block body %}
    <div class="box-header">Liste Type Droit PTAC</div><br>

    <table class="table liste">
        <thead>
            <tr>
                <th>Id</th>
                <th>libellé</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_type_droit_p_t_a_c in ct_type_droit_p_t_a_cs %}
            <tr>
                <td>{{ ct_type_droit_p_t_a_c.id }}</td>
                <td>{{ ct_type_droit_p_t_a_c.tpDpLibelle }}</td>
                <td>
                    <a href="{{ path('app_ct_type_droit_p_t_a_c_show', {'id': ct_type_droit_p_t_a_c.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('app_ct_type_droit_p_t_a_c_edit', {'id': ct_type_droit_p_t_a_c.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <div class="navigation">
        {{ knp_pagination_render(ct_type_droit_p_t_a_cs) }}
    </div> #}

    <a href="{{ path('app_ct_type_droit_p_t_a_c_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Ajouter</a>
{% endblock %}
