{% extends 'base.html.twig' %}

{% block title %}Ajout Province{% endblock %}

{% block body %}
    <div class="box-header">Ajout Province</div><br>

    {{ include('ct_province/_form.html.twig') }}
            </td>
            <td style="width:150px;">
    <a href="{{ path('app_ct_province_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}
