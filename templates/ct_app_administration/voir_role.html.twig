{% extends 'base.html.twig' %}

{% block title %}role{% endblock %}

{% block app_ct %}
<div class="box">
    <div class="box-header">ROLE</div><br>

        <table class="table">
            <tbody>
                <tr>
                    <th>Rôle</th>
                    <td>{{ ct_role.roleName }}</td>
                </tr>
            </tbody>
        </table>
        
        <table class="table" style="width:450px;">
            <tbody>
                <tr>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_administration_liste_role') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                    </td>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_administration_edit_role', {'id': ct_role.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                    </td>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_administration_del_role', {'id': ct_role.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
