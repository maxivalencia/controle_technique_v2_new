{% extends 'base.html.twig' %}

{% block title %}Création rôle{% endblock %}

{% block app_ct %}
<div class="box">
    <div class="box-header">CREER UN ROLE</div><br>

            {{ include('ct_user/_form.html.twig') }}
                    </td>
                    <td style="width:150px;">
            <a href="{{ path('app_ct_app_administration_liste_role') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
