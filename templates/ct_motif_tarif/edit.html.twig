{% extends 'base.html.twig' %}

{% block title %}Edition Tarif Motif{% endblock %}

{% block body %}
    <div class="box-header">Edition Tarif Motif</div><br>

    {{ include('ct_motif_tarif/_form.html.twig', {'button_label': 'Modifier'}) }}
            </td>
            <td style="width:150px;">
    <a href="{{ path('app_ct_motif_tarif_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
            </td>
            <td style="width:150px;">
    {{ include('ct_motif_tarif/_delete_form.html.twig') }}
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}