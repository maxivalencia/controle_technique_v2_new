{% extends 'base.html.twig' %}

{% block title %}Procès-Verbal{% endblock %}

{% block body %}
    <div class="box-header">Procès-Verbal</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_proces_verbal.id }}</td>
            </tr>
            <tr>
                <th>type</th>
                <td>{{ ct_proces_verbal.pvType }}</td>
            </tr>
            <tr>
                <th>tarif</th>
                <td>{{ ct_proces_verbal.pvTarif }}</td>
            </tr>
        </tbody>
    </table>
	
	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_proces_verbal_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_proces_verbal_edit', {'id': ct_proces_verbal.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_proces_verbal/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
