{% extends 'base.html.twig' %}

{% block title %}Motif{% endblock %}

{% block body %}
    <div class="box-header">Motif</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_motif.id }}</td>
            </tr>
            <tr>
                <th>libellé</th>
                <td>{{ ct_motif.mtfLibelle }}</td>
            </tr>
            <tr>
                <th>est-calculable</th>
                <td>{{ ct_motif.mtfIsCalculable ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>

	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_motif_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_motif_edit', {'id': ct_motif.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_motif/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
