<form method="post" action="{{ path('app_ct_autre_delete', {'id': ct_autre.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_autre.id) }}">
    <button class="bt btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</button>
</form>
