{% extends 'base.html.twig' %}

{% block title %}Edition Rôle{% endblock %}

{% block body %}
    <div class="box-header">Edition Rôle</div><br>

    {{ include('ct_role/_form.html.twig', {'button_label': 'Modifier'}) }}
            </td>
            <td style="width:150px;">
    <a href="{{ path('app_ct_role_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
            </td>
            <td style="width:150px;">
    {{ include('ct_role/_delete_form.html.twig') }}
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}