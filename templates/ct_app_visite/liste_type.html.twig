{% extends 'base.html.twig' %}

{% block title %}Liste des types{% endblock %}

{% block app_ct %}

<div class="box">
    <div class="box-header">TOTAL TYPE VISITE : {{ total }}</div><br>
    <div>
        <div>
            <a href="{{ path('app_ct_app_visite_creer_type') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer un type de visite</a>
        </div>
        <div class="marge-elts">
            <table class="table liste">
                <thead>
                    <tr>
                        <th>Libellé</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for ct_type_visite in ct_type_visites %}
                    <tr>
                        <td>{{ ct_type_visite.tpvLibelle }}</td>
                        <td>
                            <a href="{{ path('app_ct_app_visite_voir_type', {'id': ct_type_visite.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                            <a href="{{ path('app_ct_app_visite_edit_type', {'id': ct_type_visite.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">Aucun enregistrement trouvé</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div>
            <a href="{{ path('app_ct_app_visite_creer_type') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer un type de visite</a>
        </div>  
    </div>
</div>
{% endblock %}
