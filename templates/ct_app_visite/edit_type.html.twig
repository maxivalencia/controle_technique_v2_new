{% extends 'base.html.twig' %}

{% block title %}Edition Type de Visite{% endblock %}

{% block app_ct %}
<div class="box">
    <div class="box-header">EDITION  TYPE DE VISITE</div><br>

            {{ include('ct_type_visite/_form.html.twig', {'button_label': 'Modifier'}) }}
                    </td>
                    <td style="width:150px;">
            <a href="{{ path('app_ct_app_visite_liste_type') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                    </td>
                    <td style="width:150px;">
            <a href="{{ path('app_ct_app_visite_del_type', {'id': ct_type_visite.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
