{% extends 'base.html.twig' %}

{% block title %}Création des types{% endblock %}

{% block app_ct %}
<div class="box">
    <div class="box-header">CREER UN TYPE DE VISITE</div><br>

            {{ include('ct_type_visite/_form.html.twig') }}
                    </td>
                    <td style="width:150px;">
            <a href="{{ path('app_ct_app_visite_liste_type') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
