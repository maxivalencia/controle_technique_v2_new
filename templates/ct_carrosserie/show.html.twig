{% extends 'base.html.twig' %}

{% block title %}Carrosserie{% endblock %}

{% block body %}
    <div class="box-header">Carrosserie</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_carrosserie.id }}</td>
            </tr>
            <tr>
                <th>libellé</th>
                <td>{{ ct_carrosserie.crsLibelle }}</td>
            </tr>
        </tbody>
    </table>
	
	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_carrosserie_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_carrosserie_edit', {'id': ct_carrosserie.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_carrosserie/_delete_form.html.twig') }} 
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
