{% extends 'base.html.twig' %}

{% block title %}Type Constatation{% endblock %}

{% block app_ct %}
<div class="box">
    <div class="box-header">TYPE CONSTATATION</div><br>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ ct_const_av_ded_type.id }}</td>
                </tr>
                <tr>
                    <th>libellé</th>
                    <td>{{ ct_const_av_ded_type.cadTpLibelle }}</td>
                </tr>
            </tbody>
        </table>
        
        <table class="table" style="width:450px;">
            <tbody>
                <tr>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_constatation_liste_type') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                    </td>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_constatation_edit_type', {'id': ct_const_av_ded_type.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                    </td>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_constatation_del_type', {'id': ct_const_av_ded_type.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
