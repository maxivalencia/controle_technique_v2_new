{% extends 'base.html.twig' %}

{% block title %}Liste des types{% endblock %}

{% block app_ct %}

<div class="box">
    <div class="box-header">TOTAL TYPE CONSTATATION : {{ total }}</div><br>
    <div>
        <div>
            <a href="{{ path('app_ct_app_constatation_creer_type') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer un type de constatation</a>
        </div>
        <div class="marge-elts">
            <table class="table liste">
                <thead>
                    <tr>
                        <th>Libellé</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for ct_const_av_ded_type in ct_const_av_ded_types %}
                    <tr>
                        <td>{{ ct_const_av_ded_type.cadTpLibelle }}</td>
                        <td>
                            <a href="{{ path('app_ct_app_constatation_voir_type', {'id': ct_const_av_ded_type.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                            <a href="{{ path('app_ct_app_constatation_edit_type', {'id': ct_const_av_ded_type.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">Aucun enregistrement trouvé</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div>
            <a href="{{ path('app_ct_app_constatation_creer_type') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Créer un type de constatation</a>
        </div>  
    </div>
</div>
{% endblock %}
