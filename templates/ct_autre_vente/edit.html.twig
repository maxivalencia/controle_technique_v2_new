{% extends 'base.html.twig' %}

{% block title %}Edition Autre Vente{% endblock %}

{% block body %}
    <div class="box-header">Edition Autre Vente</div><br>

    {{ include('ct_autre_vente/_form.html.twig', {'button_label': 'Modifier'}) }}
            </td>
            <td style="width:150px;">
    <a href="{{ path('app_ct_autre_vente_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
            </td>
            <td style="width:150px;">
    {{ include('ct_autre_vente/_delete_form.html.twig') }}
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}