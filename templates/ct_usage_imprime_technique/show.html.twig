{% extends 'base.html.twig' %}

{% block title %}Utilisation Imprime Technique{% endblock %}

{% block body %}
    <div class="box-header">Utilisation Imprime Technique</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_usage_imprime_technique.id }}</td>
            </tr>
            <tr>
                <th>libellé</th>
                <td>{{ ct_usage_imprime_technique.uitLibelle }}</td>
            </tr>
        </tbody>
    </table>
	
	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_usage_imprime_technique_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_usage_imprime_technique_edit', {'id': ct_usage_imprime_technique.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_usage_imprime_technique/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
