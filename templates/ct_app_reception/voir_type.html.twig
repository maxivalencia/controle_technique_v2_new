{% extends 'base.html.twig' %}

{% block title %}Type Reception{% endblock %}

{% block app_ct %}
<div class="box">
    <div class="box-header">TYPE RECEPTION</div><br>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ ct_type_reception.id }}</td>
                </tr>
                <tr>
                    <th>libellé</th>
                    <td>{{ ct_type_reception.tprcpLibelle }}</td>
                </tr>
            </tbody>
        </table>
        
        <table class="table" style="width:450px;">
            <tbody>
                <tr>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_reception_liste_type') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                    </td>
                    <td style="width:150px;">
                        <a href="{{ path('app_ct_app_reception_edit_type', {'id': ct_type_reception.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                    </td>
                    <td style="width:150px;">
                        {# {{ include('ct_type_visite/_delete_form.html.twig') }} #}
                        <a href="{{ path('app_ct_app_reception_del_type', {'id': ct_type_reception.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
