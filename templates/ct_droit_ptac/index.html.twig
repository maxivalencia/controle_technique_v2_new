{% extends 'base.html.twig' %}

{% block title %}Liste Droit PTAC{% endblock %}

{% block body %}
    <div class="box-header">Liste Droit PTAC</div><br>

    <table class="table liste">
        <thead>
            <tr>
                <th>Id</th>
                <th>prix minimum</th>
                <th>prix maximum</th>
                <th>droit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_droit_p_t_a_c in ct_droit_p_t_a_cs %}
            <tr>
                <td>{{ ct_droit_p_t_a_c.id }}</td>
                <td>{{ ct_droit_p_t_a_c.dpPrixMin }}</td>
                <td>{{ ct_droit_p_t_a_c.dpPrixMax }}</td>
                <td>{{ ct_droit_p_t_a_c.dpDroit }}</td>
                <td>
                    <a href="{{ path('app_ct_droit_p_t_a_c_show', {'id': ct_droit_p_t_a_c.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i></a>
                    <a href="{{ path('app_ct_droit_p_t_a_c_edit', {'id': ct_droit_p_t_a_c.id}) }}" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <div class="navigation">
        {{ knp_pagination_render(ct_droit_p_t_a_cs) }}
    </div> #}

    <a href="{{ path('app_ct_droit_p_t_a_c_new') }}" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Ajouter</a>
{% endblock %}
