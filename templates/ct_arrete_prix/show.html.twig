{% extends 'base.html.twig' %}

{% block title %}Arrêté Prix{% endblock %}

{% block body %}
    <div class="box-header">Arrêté Prix</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_arrete_prix.id }}</td>
            </tr>
            <tr>
                <th>numéro</th>
                <td>{{ ct_arrete_prix.artNumero }}</td>
            </tr>
            <tr>
                <th>date</th>
                <td>{{ ct_arrete_prix.artDate ? ct_arrete_prix.artDate|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>date d'application</th>
                <td>{{ ct_arrete_prix.artDateApplication ? ct_arrete_prix.artDateApplication|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>created_at</th>
                <td>{{ ct_arrete_prix.artCreatedAt ? ct_arrete_prix.artCreatedAt|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>updated_at</th>
                <td>{{ ct_arrete_prix.artUpdatedAt ? ct_arrete_prix.artUpdatedAt|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>observation</th>
                <td>{{ ct_arrete_prix.artObservation }}</td>
            </tr>
        </tbody>
    </table>
	
	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_arrete_prix_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_arrete_prix_edit', {'id': ct_arrete_prix.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_arrete_prix/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
