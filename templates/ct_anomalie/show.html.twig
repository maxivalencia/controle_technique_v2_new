{% extends 'base.html.twig' %}

{% block title %}Anomalie{% endblock %}

{% block body %}
    <div class="box-header">Anomalie</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_anomalie.id }}</td>
            </tr>
            <tr>
                <th>libellé</th>
                <td>{{ ct_anomalie.anmlLibelle }}</td>
            </tr>
            <tr>
                <th>code</th>
                <td>{{ ct_anomalie.anmlCode }}</td>
            </tr>
            <tr>
                <th>niveau de danger</th>
                <td>{{ ct_anomalie.anmlNiveauDanger }}</td>
            </tr>
        </tbody>
    </table>

    <table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_anomalie_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_anomalie_edit', {'id': ct_anomalie.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_anomalie/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
