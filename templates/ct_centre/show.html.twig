{% extends 'base.html.twig' %}

{% block title %}Centre{% endblock %}

{% block body %}
    <div class="box-header">Centre</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_centre.id }}</td>
            </tr>
            <tr>
                <th>nom</th>
                <td>{{ ct_centre.ctrNom }}</td>
            </tr>
            <tr>
                <th>code</th>
                <td>{{ ct_centre.ctrCode }}</td>
            </tr>
            <tr>
                <th>created_at</th>
                <td>{{ ct_centre.ctrCreatedAt ? ct_centre.ctrCreatedAt|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>updated_at</th>
                <td>{{ ct_centre.ctrUpdatedAt ? ct_centre.ctrUpdatedAt|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>Centre mère</th>
                <td>{{ ct_centre.centreMere }}</td>
            </tr>
        </tbody>
    </table>
	
	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_centre_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_centre_edit', {'id': ct_centre.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_centre/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
