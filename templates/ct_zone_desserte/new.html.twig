{% extends 'base.html.twig' %}

{% block title %}Ajout Zone Desserté{% endblock %}

{% block body %}
    <div class="box-header">Ajout Zone Desserté</div><br>

    {{ include('ct_zone_desserte/_form.html.twig') }}
            </td>
            <td style="width:150px;">
    <a href="{{ path('app_ct_zone_desserte_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}
