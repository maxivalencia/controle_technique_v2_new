{% extends 'base.html.twig' %}

{% block title %}Zone Desserté{% endblock %}

{% block body %}
    <div class="box-header">Zone Desserté</div><br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_zone_desserte.id }}</td>
            </tr>
            <tr>
                <th>libellé</th>
                <td>{{ ct_zone_desserte.zdLibelle }}</td>
            </tr>
        </tbody>
    </table>
	
	<table class="table" style="width:450px;">
        <tbody>
            <tr>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_zone_desserte_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
                </td>
                <td style="width:150px;">
                    <a href="{{ path('app_ct_zone_desserte_edit', {'id': ct_zone_desserte.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
                </td>
                <td style="width:150px;">
                    {{ include('ct_zone_desserte/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
